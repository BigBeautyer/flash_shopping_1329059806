# AI 闪购运营中台 — 产品介绍

## 一句话定位

以 **Multi-Agent** 为核心，将闪购运营的选品、定价、营销、审核、监控、诊断、复盘全流程产品化，构建可配置工作流、推荐系统和数据闭环，提升活动效率、ROI 与运营标准化水平。

---

## 1. 产品背景

闪购业务运营面临四大核心痛点：

| 痛点 | 现状 | 影响 |
|------|------|------|
| **选品慢** | 运营需比对抗 5+ 数据源，单次选品耗时 2-3 天 | 活动筹备周期长，错失市场窗口 |
| **策略不稳定** | 不同运营经验差异大，动销率波动大 | 活动效果不可预测，难以规模化 |
| **异常发现晚** | 转化率、曝光异常靠人盯盘，定位耗时 2 小时+ | 问题响应滞后，损失已发生 |
| **复盘难沉淀** | 复盘靠 Excel 和口口相传，无法形成可复制 SOP | 经验流失，新人上手慢 |

本平台通过 AI Agent 协作 + 人机协同模式，系统性地解决上述问题。

---

## 2. 核心子系统

### 2.1 AI 闪购工作流中台

活动创建和管理的一站式入口。运营只需描述活动目标（拉新 / 清库存 / 提 GMV / 提 ROI），**Planner Agent** 自动拆解任务清单，编排下游 Agent 执行顺序，设置人审卡点。

**关键能力**：
- 活动目标 → 任务 DAG 自动拆解
- 全流程可视化追踪（进度条 + 状态节点）
- 2 个人审卡点（选品审核 + 终审），运营保留最终决策权
- 驳回重做循环，支持逐商品部分通过

### 2.2 智能选品与定价系统

**选品 Agent** 基于 5 类特征（商圈 / 商品 / 用户需求 / 竞品 / 场景）进行打分排序：

- **规则引擎**：5 维加权评分（销售潜力、毛利空间、竞争稀缺性、季节匹配、库存健康）
- **LightGBM 排序模型**：~20 维特征，GMV/CVR/Margin 复合目标训练，输出特征重要性可解释
- **RAG 知识注入**：检索历史 Playbook 和商圈经验，增强推荐理由

**定价 Agent** 两层设计：

| 层级 | 说明 |
|------|------|
| 规则层（硬约束） | 毛利下限保护、价格带约束、爆款改价二次确认、高波动价格强制审批 |
| 智能层（LLM 辅助） | 价格弹性估计、分商圈差异化定价、分时段调价策略、ROI 预估 |

### 2.3 运营诊断 Agent

活动上线后实时监控，发现异常并归因。

**检测机制**：Z-score 统计带 + 绝对值底线 + 日环比降幅，覆盖 13 个指标、4 级严重度。

**漏斗分层归因**：

| 异常层级 | 检测方法 | 归因方向 |
|----------|----------|----------|
| 曝光下滑 | EWMA / Z-score | 预算不足 / 渠道分发异常 / 排位下降 / 大盘流量波动 |
| CTR 下滑 | EWMA / Z-score | 文案吸引力不足 / 素材问题 / 价格不具竞争力 / 人群错配 |
| 加购率下滑 | EWMA / Z-score | 商品卖点弱 / 评价差 / 价格敏感 / 竞品打折 |
| 支付转化下滑 | EWMA / Z-score | 库存不足 / 优惠券异常 / 结算链路问题 / 配送时效 |

先通过规则 + 时序模型定位到具体漏斗层级，再由 LLM 做归因解释和建议生成。

### 2.4 数据分析与实验平台

**漏斗看板**：12 北极星指标 + 6 护栏指标，支持指标卡 / 漏斗图 / GMV 趋势 / 各层趋势。

**AB 实验框架**：Welch's t-test + Cohen's d 效应量 + Bootstrap 置信区间，支持商圈级分流、多商圈两两对比矩阵热力图。

**活动复盘**：自动采集实际指标 vs 目标，结构化分析（what_worked / what_didnt_work / surprises），提取可复用策略自动写入 Playbook 库。

**Agent 执行回放**：完整审计日志 + 时间线可视化，可追溯每个 Agent 的决策依据和置信度。

---

## 3. Agent 工作流

```
创建活动 → Planner → 选品Agent → [人审卡点1] → 定价Agent → 文案Agent
                                                    → 审核Agent → [人审卡点2] → 上线
                                                                                      ↓
复盘Agent ←──────────────────────── 活动结束 ←── 诊断Agent（常驻监控）←──────────┘
```

**7 个 Agent 角色**：Planner（编排器）、选品、定价、文案、审核、诊断（常驻监控）、复盘。

### 标准化任务协议

所有 Agent 间通信使用统一的 JSON 协议，包含 task_id、workflow_id、agent_name、biz_goal、input、constraints、output_schema、confidence_threshold、human_review_required 等字段。

### 统一输出规范

每个 Agent 输出必须包含 6 个字段：`result`（核心结果）、`evidence`（决策依据）、`confidence`（置信度）、`risk_level`（风险评级）、`recommended_action`（运营建议）、`need_human_review`（是否需要人工审核）。

---

## 4. 北极星指标体系

| 指标 | 说明 |
|------|------|
| 曝光量 | 活动商品的曝光 UV |
| 点击率 CTR | 点击 UV / 曝光 UV |
| 加购率 | 加购 UV / 点击 UV |
| 支付转化率 | 支付 UV / 加购 UV |
| 客单价 AOV | 支付金额 / 支付订单数 |
| GMV | 支付总金额 |
| ROI | GMV / 活动成本 |
| 毛利率 | (GMV - 成本) / GMV |
| 动销率 | 有销售 SKU 数 / 总 SKU 数 |
| 复购率 | 重复购买用户占比 |
| 活动筹备时长 | 从创建到上线的时间 |
| 人均管理活动数 | 每运营同时管理的活动数 |

**护栏指标**：缺货率、库存周转天数、退款率、客诉率、文案审核驳回率、AI 建议采纳率。

---

## 5. 项目目标 KPI

| 指标 | 当前基线 | 目标值 |
|------|----------|--------|
| 活动筹备周期 | 2-3 天 | 缩短 60%-85% |
| 人均管理活动数 | 2 场/月 | 8 场/月 |
| 问题定位时间 | 2 小时 | 10 分钟 |
| AI 工具采纳率 | 20% | 80%+ |
| 活动 GMV | 基线 | 环比增长 20%-35% |
| ROI | 1:3.2 | 1:4.5-4.8 |

---

## 6. 技术架构

```
┌──────────────────────────────────────────────────────────────┐
│  应用层 (Streamlit)                                            │
│  看板 │ 工作流操作台 │ 诊断面板 │ 活动复盘 │ AB 实验 │ 执行回放 │
└──────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│  Agent 编排层 (LangGraph)                                      │
│  7 个 Agent + 标准化任务协议 + 2 个人审卡点 + 条件边循环        │
└──────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│  特征与策略层                                                  │
│  商品评分(LightGBM) │ 异常检测(Z-score/EWMA) │ RAG 知识库      │
└──────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│  数据层                                                        │
│  SQLite(业务) │ ChromaDB(向量) │ 模拟数据生成器                  │
└──────────────────────────────────────────────────────────────┘
```

**技术栈**：FastAPI + LangGraph + Streamlit + SQLite + ChromaDB + LightGBM + Plotly

**LLM**：DeepSeek API（通过 ChatOpenAI base_url 兼容接入）

---

## 7. 开发阶段与当前状态

| 阶段 | 目标 | 状态 |
|------|------|------|
| Phase 0 | 基础设施 + 数据底座 + 看板 V1 | ✅ 已完成 |
| Phase 1 | MVP 核心工作流（6 Agent + 人审卡点） | ✅ 已完成 |
| Phase 2 | 智能增强（LightGBM + 诊断 + RAG） | ✅ 已完成 |
| Phase 3 | 闭环运营（复盘 + Playbook + AB + 回放） | ✅ 已完成 |

---

## 8. 快速体验

```bash
# 1. 启动后端 API
cd backend
uvicorn main:app --reload --port 8000

# 2. 启动前端
cd frontend
streamlit run app.py
```

- 前端：`http://localhost:8501`（6 个页面）
- API 文档：`http://127.0.0.1:8000/docs`

---

## 9. 设计原则

1. **不追求全自动，先做辅助决策 + 人审卡点**：模型只做推荐，运营保留最终决策权
2. **结构化决策靠规则/模型，LLM 负责理解、编排、解释和生成**：不让 LLM 做精确计算
3. **统一指标口径**：MetricsService 是唯一指标计算入口，禁止 Agent 自行计算
4. **反馈闭环**：每个 Agent 输出都有采纳/拒绝追踪，驱动模型持续优化
5. **可解释、可审计**：每个决策附带 evidence + confidence + risk_level，支持执行回放
